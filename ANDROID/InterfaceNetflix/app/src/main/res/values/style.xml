<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Estilo Primary Button -->
    <style name="BlueRoundedButton" parent="Widget.AppCompat.Button">
        <item name="android:backgroundTint">#03A9F4</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@android:color/white</item>
        <!-- <item name="android:fontFamily">@font/your_font</item> --> <!-- Fonte, se aplicável -->
        <item name="android:textSize">14sp</item>
        <item name="android:gravity">center</item>
        <!--  <item name="android:textAllCaps">false</item> -->
        <item name="android:background">@drawable/rounded_button_background</item> <!-- Arquivo XML para definir bordas arredondadas -->
    </style>

</resources>